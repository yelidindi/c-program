#include <stdio.h>

int main() {
   int n,sum=0,r;
   scanf("%d",&n);
    while(n)
   {
       r=n%10;
       sum=sum+r;
       n=n/10;
   }
   if(n/sum==0)
   printf("Harshad Number");
   else
   printf("Not Harshad Number");
   
   return 0;
}
